#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// WiFi Credentials
const char* WIFI_SSID = "your-ssid-here";
const char* WIFI_PASSWORD = "your-password-here";

// SMTP Credentials (for Gmail: create an app password at myaccount.google.com/apppasswords)
const char* SMTP_HOST = "smtp.gmail.com";
const int SMTP_PORT = 465;
const char* SMTP_USER = "your-email@gmail.com";
const char* SMTP_PASS = "your-app-password-here";  // Use app password, not regular password
const char* SMTP_FROM = "your-email@gmail.com";
const char* SMTP_TO = "recipient@gmail.com";

#endif
